import { BrowserRouter, Routes, Route } from "react-router-dom";
import

function Router(){
    <BrowserRouter>
    <Routes>
    <Route
          path="/"
          element={
            <Layout>
              <Home />
            </Layout>
          }
        />
    </Routes>
    
    </BrowserRouter>
}
